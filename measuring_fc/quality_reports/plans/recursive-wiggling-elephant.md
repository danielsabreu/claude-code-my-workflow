# Plan: Workflow Configuration — Financial Cycle Paper
**Status:** DRAFT  
**Date:** 2026-05-14  
**Session goal:** Adapt the Claude Code workflow configuration for the Financial Cycle paper project and create the canonical project folder structure by migrating old code.

---

## Context

This repo was cloned from the `pedrohcgs/claude-code-my-workflow` template, which was designed for **lecture slides** (Beamer + Quarto). We are repurposing it for an **academic paper project**:

- **Paper:** "Measuring the Financial Cycle: A Dynamic Factor Model Approach"
- **Authors:** Daniel Abreu (FCA) and Ivan De Lorenzo Buratta (Prometeia)
- **Target journal:** Journal of Financial Stability
- **Goal:** Publication-ready manuscript + replication package (R scripts reproducible from raw data)

All existing code lives in `old_code_and_data/` and `old_overleaf/`. These are the sources; we will migrate and reorganise them into the canonical structure. The old folders are preserved as-is (read-only reference).

---

## Phase 1 — Update CLAUDE.md

**File:** `CLAUDE.md`

Replace all `[BRACKETED PLACEHOLDERS]` and adapt sections for a paper project:

| Placeholder | Value |
|---|---|
| `[YOUR PROJECT NAME]` | Measuring the Financial Cycle: A Dynamic Factor Model Approach |
| `[YOUR INSTITUTION]` | Financial Conduct Authority |

**Structural adaptations to CLAUDE.md:**

1. **Folder structure block** — replace slide-specific dirs (`Slides/`, `Quarto/`) with paper-specific dirs:
   ```
   paper/              # LaTeX article source (authoritative)
   scripts/R/          # R analysis pipeline (numbered 01–05)
   scripts/R/auxiliary/# Helper functions
   scripts/R/_outputs/ # Generated data + figures (gitignored)
   data/raw/           # Input data snapshots (gitignored)
   Figures/            # Final publication figures (tracked in git)
   ```
   Keep: `.claude/`, `Bibliography_base.bib`, `quality_reports/`, `explorations/`, `templates/`

2. **Commands block** — replace XeLaTeX/Quarto commands with:
   ```bash
   # LaTeX (article class — pdflatex, 3 passes + bibtex)
   cd paper && pdflatex -interaction=nonstopmode main.tex
   bibtex main
   pdflatex -interaction=nonstopmode main.tex
   pdflatex -interaction=nonstopmode main.tex

   # Run full R pipeline
   Rscript scripts/R/01_extract_data.R
   Rscript scripts/R/02_data_processing.R
   Rscript scripts/R/03_factor_estimation.R
   Rscript scripts/R/04_decomp_ew.R
   Rscript scripts/R/05_oos.R

   # Quality score
   python scripts/quality_score.py paper/main.tex
   ```
   Remove: `sync_to_docs.sh`, `check-palette-sync.sh`, `check-surface-sync.sh`

3. **Core Principles** — change "Single source of truth — Beamer `.tex` is authoritative" → "Single source of truth — `paper/main.tex` is authoritative; all figures/tables are generated by R scripts"

4. **Beamer Custom Environments table** — replace with paper-specific LaTeX environments:
   | Environment | Effect | Use Case |
   |---|---|---|
   | `\hl{...}` (soul) | Yellow highlight | TODO/review markers (remove before submission) |
   | `{\color{blue}...}` | Blue text | Author comments (remove before submission) |
   | `figure[htbp]` + `adjustbox` | Resizable figure | All paper figures |
   | `tabularx` + `adjustbox` | Full-width table | Main results tables |

5. **Quarto CSS Classes table** — replace with R figure standards:
   | Variable | Value | Use Case |
   |---|---|---|
   | `fig_width` | 12 | Standard full-width figure |
   | `fig_height` | 5 | Standard figure height |
   | `panel_cols` | 2–4 | Country panel grid figures |

6. **Current Project State table** — replace lecture rows with:
   | Component | File | Status | Notes |
   |---|---|---|---|
   | Paper (LaTeX) | `paper/main.tex` | Migrated | Needs author info + cleanup |
   | Data extraction | `scripts/R/01_extract_data.R` | Migrated | Paths relative; BIS + OECD APIs |
   | Data processing | `scripts/R/02_data_processing.R` | Migrated | BW filter + normalise |
   | Factor estimation | `scripts/R/03_factor_estimation.R` | Migrated | DFM: PCA / QML / two-step |
   | Decomp + EW | `scripts/R/04_decomp_ew.R` | Migrated | Crisis detection, AUROC |
   | OOS exercise | `scripts/R/05_oos.R` | Migrated | Pseudo real-time logit |

7. **Remove** slide-specific skills from Quick Reference that don't apply to paper:  
   `/deploy`, `/extract-tikz`, `/qa-quarto`, `/translate-to-quarto`, `/pedagogy-review`, `/visual-audit`, `/slide-excellence`, `/devils-advocate`, `/create-lecture`  
   Keep: `/compile-latex`, `/review-paper`, `/review-r`, `/data-analysis`, `/audit-reproducibility`, `/lit-review`, `/validate-bib`, `/verify-claims`, `/seven-pass-review`, `/respond-to-referees`, `/commit`, `/proofread`

---

## Phase 2 — Update `.claude/rules/r-code-conventions.md`

Replace the template institutional palette with a publication-appropriate neutral palette suitable for Journal of Financial Stability:

```r
# --- Publication palette (neutral academic, JFS-appropriate) ---
primary_blue   <- "#003366"   # deep navy — main series
accent_red     <- "#CC3333"   # crisis / warning signals
accent_gray    <- "#666666"   # secondary series
band_fill      <- "#CCDDEE"   # confidence band fill
positive_green <- "#2E7D32"   # positive annotations
```

Add paper-specific notes:
- DFM estimation via `dfms` package (`DFM()` function)
- Factor sign normalization: flip sign so factor correlates positively with first PC
- Country loop pattern: `lapply(names(df_model), function(country) {...})`
- `set.seed(20240101)` at top of any script with random elements
- Output dir: `scripts/R/_outputs/` (create with `dir.create(..., recursive = TRUE)`)

---

## Phase 3 — Create Folder Structure and Migrate Files

### 3a. Directories to create
```
paper/
paper/sections/
paper/appendices/
paper/figures/          # final figures (tracked)
paper/tables/           # final tables (tracked if .tex, not if .xlsx)
scripts/R/auxiliary/
scripts/R/_outputs/     # gitignored
data/raw/               # gitignored (contains large RData / xlsx)
Figures/                # kept for template compat (can symlink)
```

### 3b. LaTeX migration: `old_overleaf/` → `paper/`

| Source | Destination |
|---|---|
| `old_overleaf/main.tex` | `paper/main.tex` (update \input paths) |
| `old_overleaf/1. INTRO.tex` | `paper/sections/01_intro.tex` |
| `old_overleaf/2. DATA.tex` | `paper/sections/02_data.tex` |
| `old_overleaf/3. METHODOLOGY.tex` | `paper/sections/03_methodology.tex` |
| `old_overleaf/4. RESULTS.tex` | `paper/sections/04_results.tex` |
| `old_overleaf/5. CONCLUSION.tex` | `paper/sections/05_conclusion.tex` |
| `old_overleaf/APPENDIX_A_FC.tex` | `paper/appendices/A_factor_estimates.tex` |
| `old_overleaf/APPENDIX_B_DEC.tex` | `paper/appendices/B_decomposition.tex` |
| `old_overleaf/APPENDIX_C_CI.tex` | `paper/appendices/C_confidence_intervals.tex` |
| `old_overleaf/REFERENCES.bib` | `paper/bibliography.bib` (also update `Bibliography_base.bib`) |
| `old_overleaf/TABLES_FIGURES/*.png` | `paper/figures/` (temporary; will be regenerated) |

`paper/main.tex` must be updated to:
- Use `\input{sections/01_intro}` etc. (new paths)
- Fix duplicate `\footnote{\footnote{...}}` in methodology (typo in original)
- Fill author affiliation: Daniel Abreu — Financial Conduct Authority

### 3c. R scripts migration: `old_code_and_data/.../CLEAN_VERSION/` → `scripts/R/`

| Source | Destination |
|---|---|
| `CLEAN_VERSION/1_EXTRACT_DATA.R` | `scripts/R/01_extract_data.R` |
| `CLEAN_VERSION/2_DATA_PROCESSING.R` | `scripts/R/02_data_processing.R` |
| `CLEAN_VERSION/3_FACTOR_ESTIMATION.R` | `scripts/R/03_factor_estimation.R` |
| `CLEAN_VERSION/4_DEC_EW_GAR.R` | `scripts/R/04_decomp_ew.R` |
| `CLEAN_VERSION/5_OOS.R` | `scripts/R/05_oos.R` |

Each migrated script needs these minimal fixes (do not rewrite — just make it loadable):
1. Remove `setwd("G:/6.APM/...")` — replace with `here::here()` or relative paths from repo root
2. Remove proxy settings (`Sys.setenv(http_proxy = "proxy.bportugal.pt:8080")`)
3. Update all `load("G:/6.APM/...")` → `load(here::here("data/raw/..."))` or `load(here::here("scripts/R/_outputs/..."))`
4. Update all `save(..., file = "2. Data/...")` → `save(..., file = here::here("scripts/R/_outputs/..."))`
5. Update `source("G:/6.APM/...")` → `source(here::here("scripts/R/auxiliary/..."))`

### 3d. Auxiliary functions migration

All files from `old_code_and_data/Financial Cycle/3. Codes/Auxiliary/*.R` → `scripts/R/auxiliary/`

Files: `filterhp.R`, `trend_filterHP.R`, `getfilter.R`, `normalise.R`, `getseas.R`, `getseas_v2.R`, `transform_2yoy.R`, `transform.R`, `ICr_c.R`, `avar.R`, `dfm_conf_int.R`, `FC_decomposition.R`, `getauroc.R`, `get_oos_auroc.R`, `get_coords.R`, `urtests.R`, `boundaryFstats.R`, `main_with_pass.R`, `BW_filter.R`, `create_grid.R`

### 3e. Data migration: `old_code_and_data/.../2. Data/` → `data/raw/`

Commit these snapshot files (needed for replication without running script 01):
- `df_country.RData` — main processed dataset
- `esrb.fcdb20220120.en.xlsx` — ESRB crisis database
- `SRIs_20180530_including_readme.xlsx` — CCyB taskforce DSR backcast
- `shadow_rates.xlsx`

Do NOT commit `.RData` files that are intermediate outputs (regeneratable by running the pipeline).

---

## Phase 4 — Update .gitignore

Add:
```
# R pipeline outputs (regenerated by scripts)
scripts/R/_outputs/

# Raw data (large files; included selectively)
data/raw/*.RData
# But track these specific snapshot files:
!data/raw/df_country.RData
!data/raw/esrb.fcdb20220120.en.xlsx
!data/raw/SRIs_20180530_including_readme.xlsx
!data/raw/shadow_rates.xlsx

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.synctex.gz
paper/*.out
paper/*.toc
```

---

## Phase 5 — Save Project and User Memories

Files to create in `.claude/projects/C--Users-danie-OneDrive-Desktop-my-project/memory/`:

1. `user_daniel.md` — Daniel Abreu, FCA, macroprudential policy researcher, financial econometrician; co-author on Financial Cycle paper; wants structured/rigorous workflow with memory of decisions
2. `project_financial_cycle.md` — paper title, co-authors, JFS target, replication package goal, 5-script pipeline, European country sample (BE/DE/ES/FI/FR/GB/IT/NL/PT/SE/NO/DK)
3. `feedback_workflow_style.md` — structured, precise, rigorous; polished publication-ready visuals; remembers decisions; check-in frequently in early sessions

Update `MEMORY.md` with pointers to all three.

---

## Phase 6 — Write Initial Session Log

`quality_reports/session_logs/2026-05-14_workflow-setup.md`

Captures: goal, approach, key decisions (pdflatex not xelatex; JFS target; snapshot data included; auxiliary functions available).

---

## Verification

After all phases complete:
1. `grep -r "\[YOUR" CLAUDE.md` → 0 matches (no unfilled placeholders)
2. `ls paper/` shows `main.tex`, `sections/`, `appendices/`, `bibliography.bib`, `figures/`
3. `ls scripts/R/` shows `01_extract_data.R` through `05_oos.R` + `auxiliary/`
4. `ls data/raw/` shows the 4 committed data files
5. Compile test: `cd paper && pdflatex -interaction=nonstopmode main.tex` → no missing file errors
6. R path check: `grep -r "G:/6.APM" scripts/R/` → 0 matches

---

## What this plan does NOT include (future sessions)

- Full R script rewrite to project standards (set.seed, ggplot2 consistency, proper output patterns, reproducibility hardening) — this is `/review-r` + `/data-analysis` work
- LaTeX paper rewrite (clean up `\hl{}` highlights, `{\color{blue}` TODO comments, finalize author info, fix double `\footnote{}` nesting) — this is `/proofread` + `/review-paper` work
- Figure regeneration (running the R pipeline to produce publication-quality figures)
- Full `/review-paper --peer Journal of Financial Stability` pass
